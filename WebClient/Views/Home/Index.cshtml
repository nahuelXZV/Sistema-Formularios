@using WebClient.Components
@using WebClient.Components.General.Home

@{
	ViewData["Title"] = "Home Page";
	ViewData["Module"] = "Home";
	ViewData["Action"] = "Dashboard";
	ViewData["Url"] = Url.Action("Index", "Home");

	var componentName = nameof(HomeComponent);
	var componentParameters = new Dictionary<string, object>
	{
		// { nameof(FeriadoCrearComponent.Feriado), Model.Feriado  },
	};
}

@section Styles {

}

@section ContentHeader {
	<nav class="flex">

	</nav>
}
<div class="rounded-2xl border border-gray-200 bg-white px-3 py-3">
	<component type="typeof(MainComponent)"
			   param-ComponentTypeName="typeof(HomeComponent).FullName"
			   param-Parameters="@componentParameters"
			   render-mode="ServerPrerendered" />
</div>
